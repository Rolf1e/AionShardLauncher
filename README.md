Aion Shard Launcher require `config.txt` file to store users preferences

- You can build the project and execute `AionShardLauncher.exe` 
Don't forget to put in the same file `RestSharp.dll` and `config.txt`